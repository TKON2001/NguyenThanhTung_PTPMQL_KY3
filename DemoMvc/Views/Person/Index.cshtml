@model IEnumerable<DemoMvc.Models.Person>
@{
    ViewData["Title"] = "Trang chủ Person";
}

<h1>Dữ liệu từ Controller gửi về là: @ViewBag.infoPerson</h1>

<form asp-action="Index">
    <label>Nhập PersonID</label>
    <input type="text" name="PersonId">
    <label>Nhập họ tên:</label>
    <input type="text" name="FullName">
    <label>Nhập địa chỉ:</label>
    <input type="text" name="Address">
    <input type="submit" value="Gửi dữ liệu">
</form>

<h1>Index</h1>
<p>
    <a asp-action="Create" class="btn btn-success">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.First().PersonId)</th>
            <th>@Html.DisplayNameFor(model => model.First().FullName)</th>
            <th>@Html.DisplayNameFor(model => model.First().Address)</th>
            <th>@Html.DisplayNameFor(model => model.First().Email)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model)
{
        <tr>
            <td>@item.PersonId</td>
            <td>@item.FullName</td>
            <td>@item.Address</td>
            <td>@item.Email</td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PersonId" class="btn btn-primary">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.PersonId" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
